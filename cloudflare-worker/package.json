{
  "name": "catreader-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "RSS CORS proxy Worker for CatReader",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
